See environment.yml for exact library dependencies. You can use the following commands with Miniconda3 to create and activate your Python environment:

`conda env create -f environment.yml -n gendiff`

`conda activate gendiff`
